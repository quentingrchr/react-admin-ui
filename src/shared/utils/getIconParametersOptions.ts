import { IconID } from "../../types";

export default function getIconParametersOptions(): IconID[] {
  return [
    "refresh-outlined",
    "refresh-filled",
    "search-outlined",
    "search-filled",
  ];
}
